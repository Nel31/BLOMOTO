# Configuration de l'API Backend
# URL de base de l'API (sans le /api à la fin, il sera ajouté automatiquement)
VITE_API_BASE_URL=http://localhost:5000/api

# Configuration Stripe (optionnel - pour les paiements)
# Clé publique Stripe - obtenez-la sur https://dashboard.stripe.com/apikeys
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Note: Toutes les variables d'environnement doivent commencer par VITE_ pour être accessibles dans le code
# Après modification, redémarrez le serveur de développement (npm run dev)
